# ── 데이터베이스 ──
DATABASE_URL=postgresql://postgres:postgres@db:5432/meal_planner

# ── 로컬 개발용 (Docker 없이) ──
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/meal_planner

# ── SQLite 테스트용 ──
# DATABASE_URL=sqlite:///./meal_planner.db

# ── 공공 영양 API (선택) ──
FOOD_NUTRIENT_API_KEY=
